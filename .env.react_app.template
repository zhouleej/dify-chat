# React App 环境变量配置模板
# 复制此文件为 .env.react_app 并根据实际环境修改配置

# API 基础地址 - Platform 服务的客户端 API 地址
# 本地开发: http://localhost:5300/api/client
# 生产环境: https://your-platform-domain.com/api/client
PUBLIC_APP_API_BASE=http://localhost:5300/api/client

# Dify 代理 API 基础地址 - Platform 服务的 Dify 代理 API 地址
# 本地开发: http://localhost:5300/api/client/dify
# 生产环境: https://your-platform-domain.com/api/client/dify
PUBLIC_DIFY_PROXY_API_BASE=http://localhost:5300/api/client/dify

# 调试模式 - 开发环境设置为 true，生产环境设置为 false
PUBLIC_DEBUG_MODE=false

# 使用说明：
# 1. 复制此文件为 .env.react_app
# 2. 根据实际的 Platform 服务地址修改 API 基础地址
# 3. 这些变量会在容器启动时通过 replace_env.sh 脚本动态替换到 public/env.js
# 4. 如果使用 Docker Compose，可以在 docker-compose.yml 中覆盖这些值
# 5. 生产环境中请确保 API 地址指向正确的服务端点